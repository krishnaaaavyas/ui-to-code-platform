# Frontend Environment Variables

# Base URL for API requests. Defaults to "/api" in production (handled by reverse proxy)
VITE_API_BASE_URL=http://localhost:4000/api

# Endpoint for Socket.IO connections. Defaults to the window origin in production
VITE_SOCKET_URL=http://localhost:4000
